<?xml version="1.0" encoding="utf-8"?>
<!--
  The only manifest-level difference between the development build and the others: the cleartext
  opening towards the emulator host, described in `res/xml/network_security_config.xml`.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
